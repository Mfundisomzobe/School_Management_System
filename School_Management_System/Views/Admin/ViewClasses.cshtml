@model List<Class>
@{
    ViewData["Title"] = "Classes";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container-fluid px-3 px-md-4 mt-3 mt-md-4">
    <!-- Header -->
    <div class="row mb-4">
        <div class="col-12">
            <div class="d-flex flex-column flex-sm-row justify-content-between align-items-start align-items-sm-center gap-2 gap-sm-0">
                <div>
                    <h2 class="fw-bold fs-4 fs-md-3">
                        <i class="fas fa-chalkboard text-primary me-2"></i>Classes
                    </h2>
                    <nav aria-label="breadcrumb">
                        <ol class="breadcrumb small mb-0">
                            <li class="breadcrumb-item"><a asp-action="Dashboard" class="text-decoration-none">Dashboard</a></li>
                            <li class="breadcrumb-item active">Classes</li>
                        </ol>
                    </nav>
                </div>
                <div class="d-flex gap-2">
                    <a asp-action="CreateClass" class="btn btn-primary btn-sm w-100 w-sm-auto">
                        <i class="fas fa-plus me-1"></i>Add New Class
                    </a>
                </div>
            </div>
        </div>
    </div>

    <!-- Alerts -->
    @if (TempData["Success"] != null)
    {
        <div class="alert alert-success alert-dismissible fade show small">
            <i class="fas fa-check-circle me-2"></i> @TempData["Success"]
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
    }
    @if (TempData["Error"] != null)
    {
        <div class="alert alert-danger alert-dismissible fade show small">
            <i class="fas fa-exclamation-circle me-2"></i> @TempData["Error"]
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
    }

    <!-- Search & Filter -->
    <div class="row mb-3">
        <div class="col-12 col-md-8">
            <form method="get" asp-action="ViewClasses" class="d-flex gap-2 flex-wrap">
                <div class="input-group flex-grow-1">
                    <span class="input-group-text"><i class="fas fa-search"></i></span>
                    <input type="text" name="searchTerm" class="form-control" placeholder="Search classes..." value="@ViewBag.SearchTerm" />
                </div>
                <div class="d-flex gap-2 align-items-center">
                    <div class="form-check form-switch">
                        <input class="form-check-input" type="checkbox" name="showInactive" value="true"
                               id="showInactive" @(ViewBag.ShowInactive == true ? "checked" : "")
                               onchange="this.form.submit()" />
                        <label class="form-check-label" for="showInactive">Show Inactive</label>
                    </div>
                    <button type="submit" class="btn btn-primary btn-sm">Search</button>
                    <a asp-action="ViewClasses" class="btn btn-secondary btn-sm">Reset</a>
                </div>
            </form>
        </div>
    </div>

    <!-- Classes Table -->
    <div class="card shadow">
        <div class="card-header bg-white py-2 py-md-3">
            <h5 class="mb-0 fw-bold fs-6 fs-md-5">
                <i class="fas fa-list text-primary me-2"></i>All Classes
                <span class="badge bg-secondary ms-2">@ViewBag.TotalCount</span>
            </h5>
        </div>
        <div class="card-body p-2 p-md-3">
            @if (Model.Any())
            {
                <div class="table-responsive">
                    <table class="table table-striped table-hover mb-0" id="classTable">
                        <thead>
                            <tr>
                                <th>#</th>
                                <th>Class Name</th>
                                <th>Course</th>
                                <th class="d-none d-sm-table-cell">Teacher</th>
                                <th>Capacity</th>
                                <th>Enrolled</th>
                                <th class="d-none d-md-table-cell">Available</th>
                                <th>Status</th>
                                <th>Actions</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var classEntity in Model)
                            {
                                var enrollmentCount = classEntity.Enrollments?.Count(e => e.IsActive) ?? 0;
                                var availableSpots = classEntity.Capacity - enrollmentCount;
                                <tr>
                                    <td>@classEntity.ClassId</td>
                                    <td>@classEntity.ClassName</td>
                                    <td>@classEntity.Course?.CourseName</td>
                                    <td class="d-none d-sm-table-cell">@classEntity.Teacher?.User?.FullName</td>
                                    <td>@classEntity.Capacity</td>
                                    <td><span class="badge bg-info">@enrollmentCount</span></td>
                                    <td class="d-none d-md-table-cell">
                                        <span class="badge @(availableSpots > 0 ? "bg-success" : "bg-danger")">
                                            @availableSpots
                                        </span>
                                    </td>
                                    <td>
                                        @if (classEntity.IsActive)
                                        {
                                            <span class="badge bg-success"><i class="fas fa-check-circle me-1"></i>Active</span>
                                        }
                                        else
                                        {
                                            <span class="badge bg-danger"><i class="fas fa-times-circle me-1"></i>Inactive</span>
                                        }
                                    </td>
                                    <td>
                                        <div class="d-flex gap-1">
                                            @if (classEntity.IsActive)
                                            {
                                                <!-- Show Edit and Delete for Active classes -->
                                                <a asp-action="EditClass" asp-route-id="@classEntity.ClassId" class="btn btn-warning btn-sm" title="Edit">
                                                    <i class="fas fa-edit"></i>
                                                </a>
                                                <a asp-action="DeleteClass" asp-route-id="@classEntity.ClassId" class="btn btn-danger btn-sm" title="Deactivate"
                                                   onclick="return confirm('Are you sure you want to deactivate this class?');">
                                                    <i class="fas fa-trash"></i>
                                                </a>
                                            }
                                            else
                                            {
                                                <!-- Show Reactivate for Inactive classes -->
                                                <a asp-action="ReactivateClass" asp-route-id="@classEntity.ClassId" class="btn btn-success btn-sm" title="Reactivate"
                                                   onclick="return confirm('Are you sure you want to reactivate this class?');">
                                                    <i class="fas fa-undo me-1"></i>Reactivate
                                                </a>
                                            }
                                        </div>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            }
            else
            {
                <div class="text-center py-4 py-md-5">
                    <i class="fas fa-chalkboard fa-3x text-muted mb-3"></i>
                    <h5 class="text-muted">No Classes Found</h5>
                    <p class="text-muted small">
                        @(ViewBag.ShowInactive == true ? "No classes match your search criteria." : "Start by adding your first class.")
                    </p>
                    <a asp-action="CreateClass" class="btn btn-primary btn-sm">
                        <i class="fas fa-plus me-1"></i>Add New Class
                    </a>
                </div>
            }
        </div>
    </div>

    <!-- Pagination -->
    @if (ViewBag.TotalPages > 1)
    {
        <nav aria-label="Page navigation" class="mt-3">
            <ul class="pagination pagination-sm justify-content-center">
                <li class="page-item @(ViewBag.CurrentPage == 1 ? "disabled" : "")">
                    <a class="page-link" asp-action="ViewClasses" asp-route-page="@(ViewBag.CurrentPage - 1)"
                       asp-route-searchTerm="@ViewBag.SearchTerm" asp-route-showInactive="@ViewBag.ShowInactive">
                        <i class="fas fa-chevron-left"></i>
                    </a>
                </li>
                @for (int i = 1; i <= ViewBag.TotalPages; i++)
                {
                    <li class="page-item @(i == ViewBag.CurrentPage ? "active" : "")">
                        <a class="page-link" asp-action="ViewClasses" asp-route-page="@i"
                           asp-route-searchTerm="@ViewBag.SearchTerm" asp-route-showInactive="@ViewBag.ShowInactive">@i</a>
                    </li>
                }
                <li class="page-item @(ViewBag.CurrentPage == ViewBag.TotalPages ? "disabled" : "")">
                    <a class="page-link" asp-action="ViewClasses" asp-route-page="@(ViewBag.CurrentPage + 1)"
                       asp-route-searchTerm="@ViewBag.SearchTerm" asp-route-showInactive="@ViewBag.ShowInactive">
                        <i class="fas fa-chevron-right"></i>
                    </a>
                </li>
            </ul>
        </nav>
    }
</div>

@section Scripts {
    <script>
        $(document).ready(function() {
            $('#classTable').DataTable({
                responsive: true,
                paging: false,
                searching: false,
                info: false,
                columnDefs: [
                    { orderable: false, targets: [8] }
                ]
            });
        });
    </script>
}