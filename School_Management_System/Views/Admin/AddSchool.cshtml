@model School_Management_System.Models.SchoolInfo

@{
    ViewData["Title"] = "Add School";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<style>
    :root {
        --primary-blue: #0097f8;
        --secondary-blue: #00b5f8;
        --accent-blue: #60a5fa;
        --light-blue: #dbeafe;
        --dark-blue: #1e3a8a;
        --gradient-primary: linear-gradient(135deg, #0097f8 0%, #3b82f6 100%);
        --gradient-secondary: linear-gradient(135deg, #3b82f6 0%, #60a5fa 100%);
        --gradient-success: linear-gradient(135deg, #059669 0%, #10b981 100%);
        --glass-bg: rgba(255, 255, 255, 0.15);
        --glass-border: rgba(255, 255, 255, 0.2);
        --shadow-blue: 0 8px 32px 0 rgba(30, 64, 175, 0.3);
    }

    body {
        background: linear-gradient(135deg, #0097f8 0%, #0097f8 50%, #00b5f8 100%);
        min-height: 100vh;
        font-family: 'Inter', 'Segoe UI', system-ui, sans-serif;
        margin: 0;
        padding: 0;
    }
    /* Custom styles for the content area */
    body {
        background-color: #f8f9fa; /* Light background for contrast */
        font-family: 'Arial', sans-serif; /* Modern font */
    }

    .content {
        margin: 20px;
        position: relative; /* Allows positioning of buttons at the bottom */
    }

    /* Glassmorphism effect for the card */
    .card {
        background: rgba(255, 255, 255, 0.9); /* Slightly less transparent */
        backdrop-filter: blur(15px); /* Increased blur effect for depth */
        border: 1px solid rgba(255, 255, 255, 0.25); /* More prominent light border */
        border-radius: 0.5rem;
        box-shadow: 0 8px 30px rgba(0, 0, 0, 0.2); /* Deeper shadow for a more pronounced effect */
        transition: transform 0.3s ease; /* Smooth transition for hover */
        padding-bottom: 50px; /* Adds space for the buttons */
    }

        .card:hover {
            transform: translateY(-5px); /* Lift effect on hover */
        }

    .card-header {
        background: rgba(0, 123, 255, 0.85); /* More opaque blue background */
        color: white;
        padding: 15px; /* Add padding for better spacing */
        border-top-left-radius: 0.5rem; /* Rounded corners on top */
        border-top-right-radius: 0.5rem;
    }

    h4 {
        margin: 0; /* Remove margin for header */
        font-size: 1.25rem; /* Adjust font size */
    }

    .form-control {
        border: 1px solid #ced4da; /* Standard border */
        border-radius: 0.25rem; /* Standard rounded corners */
        transition: border-color 0.2s; /* Transition for focus */
    }

        .form-control:focus {
            border-color: #007bff; /* Blue border on focus */
            box-shadow: 0 0 5px rgba(0, 123, 255, 0.5); /* Glow effect on focus */
        }

    .btn-primary {
        background-color: #007bff;
        border-color: #007bff;
        padding: 10px 20px; /* Increased padding for better clickability */
        transition: background-color 0.3s, border-color 0.3s; /* Transition for hover effects */
    }

        .btn-primary:hover {
            background-color: #0056b3;
            border-color: #0056b3;
        }

    .btn-secondary {
        background-color: #6c757d; /* Gray background for secondary button */
        border-color: #6c757d;
        padding: 10px 20px; /* Consistent padding */
        transition: background-color 0.3s; /* Transition for hover effects */
    }

        .btn-secondary:hover {
            background-color: #5a6268; /* Darker gray on hover */
        }

    .text-danger {
        font-size: 0.9rem; /* Smaller font size for error messages */
    }

    /* Spacing between form groups */
    .form-group {
        margin-bottom: 1.5rem; /* Increased spacing between form groups */
    }

    /* Style for positioning the buttons at the bottom */
    .button-container {
        position: absolute;
        bottom: 20px;
    }

    .back-to-list-container {
        right: 20px;
    }

    .add-button-container {
        left: 20px;
    }

    .back-to-list-btn,
    .add-btn {
        padding: 10px 20px;
    }

</style>

<div class="content">
    <div class="card mt-4">
        <div class="card-header">
            <h4 class="mb-0"><i class="fas fa-school"></i> School Info</h4>
        </div>
        <div class="card-body">
            <hr />
            <div class="row">
                <div class="col-md-6">
                    <form asp-action="AddSchool">
                        <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>
                        <div class="form-group">
                            <label asp-for=SchoolName" class="control-label">
                                <i class="fas fa-hospital-alt"></i> School Name
                            </label>
                            <input asp-for="SchoolName" class="form-control" placeholder="Enter school name" />
                            <span asp-validation-for="SchoolName" class="text-danger"></span>
                        </div>
                        <div class="form-group">
                            <label asp-for="Address" class="control-label">
                                <i class="fas fa-map-marker-alt"></i> Address
                            </label>
                            <input asp-for="Address" class="form-control" placeholder="Enter address" />
                            <span asp-validation-for="Address" class="text-danger"></span>
                        </div>
                        <div class="form-group">
                            <label asp-for="PhoneNumber" class="control-label">
                                <i class="fas fa-phone-alt"></i> Phone Number
                            </label>
                            <input asp-for="PhoneNumber" class="form-control" placeholder="Enter phone number" />
                            <span asp-validation-for="PhoneNumber" class="text-danger"></span>
                        </div>
                        <div class="form-group">
                            <label asp-for="Email" class="control-label">
                                <i class="fas fa-envelope"></i> Email
                            </label>
                            <input asp-for="Email" class="form-control" placeholder="Enter email" />
                            <span asp-validation-for="Email" class="text-danger"></span>
                        </div>
                        <div class="form-group">
                            <label asp-for="WebsiteUrl" class="control-label">
                                <i class="fas fa-globe"></i> Website URL
                            </label>
                            <input asp-for="WebsiteUrl" class="form-control" placeholder="Enter website URL" />
                            <span asp-validation-for="WebsiteUrl" class="text-danger"></span>
                        </div>

                        <!-- Positioning buttons at the bottom corners -->
                        <div class="button-container add-button-container">
                            <button type="submit" class="btn btn-primary add-btn">
                                <i class="fas fa-plus"></i> Create
                            </button>
                        </div>

                        <div class="button-container back-to-list-container">
                            <a asp-action="SchoolInfo" class="btn btn-secondary back-to-list-btn">
                                <i class="fas fa-arrow-left"></i> Back to List
                            </a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>
