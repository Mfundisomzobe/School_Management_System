@using School_Management_System.ViewModels
@model AddStudentViewModel
@{
    ViewData["Title"] = "Add Student";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<style>
    /* ========================================
           MODERN ADD STUDENT STYLES
           ======================================== */

    /* ----- Page Header ----- */
    .page-header-modern {
        background: linear-gradient(135deg, #0a0e27 0%, #1a1a3e 50%, #2d1b69 100%);
        border-radius: 16px;
        padding: 28px 32px;
        margin-bottom: 28px;
        position: relative;
        overflow: hidden;
    }

        .page-header-modern::before {
            content: '';
            position: absolute;
            top: -50%;
            right: -20%;
            width: 300px;
            height: 300px;
            border-radius: 50%;
            background: rgba(99, 102, 241, 0.08);
            animation: floatOrb 8s ease-in-out infinite;
        }

        .page-header-modern::after {
            content: '';
            position: absolute;
            bottom: -30%;
            left: 10%;
            width: 200px;
            height: 200px;
            border-radius: 50%;
            background: rgba(236, 72, 153, 0.05);
            animation: floatOrb 10s ease-in-out infinite reverse;
        }


 

    .page-header-modern .header-content {
        position: relative;
        z-index: 1;
    }

    .page-header-modern h2 {
        color: white;
        font-weight: 700;
        margin: 0;
        font-size: 1.6rem;
    }

    .page-header-modern p {
        color: rgba(255,255,255,0.5);
        margin: 4px 0 0;
        font-size: 0.95rem;
    }

    .page-header-modern .breadcrumb {
        background: transparent;
        padding: 0;
        margin: 8px 0 0;
    }

    .page-header-modern .breadcrumb-item a {
        color: rgba(255,255,255,0.4);
        text-decoration: none;
        transition: color 0.3s ease;
        font-size: 0.85rem;
    }

        .page-header-modern .breadcrumb-item a:hover {
            color: #818cf8;
        }

    .page-header-modern .breadcrumb-item.active {
        color: rgba(255,255,255,0.6);
        font-size: 0.85rem;
    }

    .page-header-modern .breadcrumb-item::before {
        color: rgba(255,255,255,0.15);
    }

    /* ----- Form Card ----- */
    .form-card-modern {
        border: none;
        border-radius: 16px;
        box-shadow: 0 4px 24px rgba(0,0,0,0.04);
        overflow: hidden;
        transition: box-shadow 0.3s ease;
    }

        .form-card-modern:hover {
            box-shadow: 0 8px 40px rgba(0,0,0,0.06);
        }

        .form-card-modern .card-header {
            background: white;
            border-bottom: 1px solid #f0f2f5;
            padding: 18px 28px;
        }

        .form-card-modern .card-body {
            padding: 28px;
        }

    /* ----- Form Controls ----- */
    .form-label-modern {
        font-weight: 600;
        font-size: 0.85rem;
        color: #1a1a3e;
        margin-bottom: 6px;
    }

    .form-control-modern,
    .form-select-modern {
        border-radius: 10px;
        border: 1.5px solid #e5e7eb;
        padding: 10px 16px;
        font-size: 0.9rem;
        transition: all 0.3s ease;
        background: #fafbfc;
    }

        .form-control-modern:focus,
        .form-select-modern:focus {
            border-color: #6366f1;
            box-shadow: 0 0 0 4px rgba(99, 102, 241, 0.08);
            background: white;
        }

        .form-control-modern.is-invalid {
            border-color: #ef4444;
            box-shadow: 0 0 0 4px rgba(239, 68, 68, 0.08);
        }

    .input-group-modern .input-group-text {
        background: #f8fafc;
        border: 1.5px solid #e5e7eb;
        border-right: none;
        border-radius: 10px 0 0 10px;
        color: #6b7280;
        font-size: 0.9rem;
        padding: 10px 14px;
    }

    .input-group-modern .form-control,
    .input-group-modern .form-select {
        border-radius: 0 10px 10px 0;
        border-left: none;
    }

        .input-group-modern .form-control:focus {
            border-color: #6366f1;
        }

    /* ----- Auto-Generated Section ----- */
    .auto-gen-section {
        background: linear-gradient(135deg, #f8fafc 0%, #f1f5f9 100%);
        border-radius: 14px;
        padding: 20px 24px;
        border: 1px solid #e5e7eb;
        position: relative;
        overflow: hidden;
    }

        .auto-gen-section .section-title {
            font-weight: 600;
            color: #1a1a3e;
            margin-bottom: 16px;
            display: flex;
            align-items: center;
            gap: 10px;
            font-size: 0.95rem;
        }

            .auto-gen-section .section-title i {
                color: #6366f1;
                font-size: 1.1rem;
            }

        .auto-gen-section .credential-field {
            background: white;
            border-radius: 10px;
            padding: 10px 16px;
            border: 1px solid #e5e7eb;
            font-weight: 600;
            font-size: 0.95rem;
            font-family: 'Courier New', monospace;
            letter-spacing: 0.5px;
            color: #1a1a3e;
            width: 100%;
            background: white;
            cursor: default;
        }

        .auto-gen-section .btn-regenerate {
            border-radius: 10px;
            padding: 8px 20px;
            font-weight: 600;
            transition: all 0.3s ease;
            background: linear-gradient(135deg, #f59e0b, #d97706);
            border: none;
            color: white;
            font-size: 0.85rem;
        }

            .auto-gen-section .btn-regenerate:hover {
                transform: translateY(-2px);
                box-shadow: 0 8px 25px rgba(245, 158, 11, 0.3);
            }

            .auto-gen-section .btn-regenerate:disabled {
                opacity: 0.6;
                cursor: not-allowed;
                transform: none;
            }

        .auto-gen-section .btn-toggle-password {
            border-radius: 0 10px 10px 0;
            padding: 0 18px;
            border: 1px solid #e5e7eb;
            border-left: none;
            background: white;
            color: #6b7280;
            transition: all 0.3s ease;
        }

            .auto-gen-section .btn-toggle-password:hover {
                color: #6366f1;
                background: #f8fafc;
            }

        .auto-gen-section .input-group .credential-field {
            border-radius: 0;
            border-left: none;
            border-right: none;
        }

            .auto-gen-section .input-group .credential-field:first-child {
                border-radius: 10px 0 0 10px;
                border-left: 1px solid #e5e7eb;
            }

    /* ----- Success Alert ----- */
    .success-alert-modern {
        border: none;
        border-radius: 14px;
        padding: 20px 24px;
        background: linear-gradient(135deg, #ecfdf5 0%, #d1fae5 100%);
        border-left: 4px solid #22c55e;
        margin-bottom: 24px;
        position: relative;
        animation: slideDown 0.5s ease;
    }


    .success-alert-modern h6 {
        color: #065f46;
        font-weight: 600;
        margin: 0 0 12px 0;
    }

    .success-alert-modern .credentials-box {
        background: white;
        border-radius: 10px;
        padding: 14px 18px;
        border: 1px solid #e5e7eb;
        display: flex;
        flex-wrap: wrap;
        gap: 16px 24px;
    }

        .success-alert-modern .credentials-box .cred-item {
            display: flex;
            align-items: center;
            gap: 8px;
            flex-wrap: wrap;
        }

            .success-alert-modern .credentials-box .cred-item .label {
                font-weight: 600;
                color: #374151;
                font-size: 0.85rem;
            }

            .success-alert-modern .credentials-box .cred-item .value {
                padding: 4px 14px;
                border-radius: 6px;
                font-weight: 600;
                font-size: 0.85rem;
                font-family: 'Courier New', monospace;
            }

                .success-alert-modern .credentials-box .cred-item .value.admission {
                    background: #e0e7ff;
                    color: #4338ca;
                }

                .success-alert-modern .credentials-box .cred-item .value.pass {
                    background: #fef3c7;
                    color: #92400e;
                }

            .success-alert-modern .credentials-box .cred-item .btn-copy {
                padding: 2px 10px;
                font-size: 0.75rem;
                border-radius: 6px;
                border: 1px solid #e5e7eb;
                background: white;
                color: #6b7280;
                transition: all 0.3s ease;
                cursor: pointer;
            }

                .success-alert-modern .credentials-box .cred-item .btn-copy:hover {
                    background: #f3f4f6;
                    color: #1a1a3e;
                }

    /* ----- Action Buttons ----- */
    .action-buttons-modern {
        display: flex;
        gap: 10px;
        justify-content: flex-end;
        flex-wrap: wrap;
        padding-top: 20px;
        border-top: 1px solid #f0f2f5;
        margin-top: 20px;
    }

        .action-buttons-modern .btn {
            border-radius: 10px;
            padding: 10px 28px;
            font-weight: 600;
            transition: all 0.3s ease;
            font-size: 0.9rem;
        }

        .action-buttons-modern .btn-success {
            background: linear-gradient(135deg, #22c55e, #16a34a);
            border: none;
            color: white;
        }

            .action-buttons-modern .btn-success:hover {
                transform: translateY(-2px);
                box-shadow: 0 8px 30px rgba(34, 197, 94, 0.3);
                color: white;
            }

            .action-buttons-modern .btn-success:disabled {
                opacity: 0.7;
                cursor: not-allowed;
                transform: none;
            }

        .action-buttons-modern .btn-secondary:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 30px rgba(0,0,0,0.08);
        }

    

    /* ----- Loading Spinner ----- */
    .btn-loading {
        pointer-events: none;
        opacity: 0.7;
    }

    /* ----- Tooltip Animation ----- */
    .copy-tooltip {
        position: fixed;
        bottom: 20px;
        left: 50%;
        transform: translateX(-50%);
        background: #1a1a3e;
        color: white;
        padding: 8px 24px;
        border-radius: 10px;
        font-size: 0.9rem;
        z-index: 9999;
        box-shadow: 0 4px 20px rgba(0,0,0,0.2);
        animation: fadeInUp 0.3s ease;
        opacity: 1;
        transition: opacity 0.5s ease;
    }

  
</style>

<!-- ============================================
     PAGE HEADER
     ============================================ -->
<div class="container-fluid px-3 px-md-4 mt-3 mt-md-4">
    <div class="page-header-modern">
        <div class="header-content">
            <div class="row align-items-center">
                <div class="col-md-8">
                    <div class="d-flex align-items-center gap-3">
                        <div class="bg-success bg-opacity-10 text-success rounded-circle d-flex align-items-center justify-content-center" style="width: 52px; height: 52px; font-size: 22px;">
                            <i class="fas fa-user-graduate"></i>
                        </div>
                        <div>
                            <h2>Add New Student</h2>
                            <p>Create a new student account with auto-generated credentials</p>
                        </div>
                    </div>
                    <nav aria-label="breadcrumb">
                        <ol class="breadcrumb">
                            <li class="breadcrumb-item"><a asp-action="Dashboard">Dashboard</a></li>
                            <li class="breadcrumb-item"><a asp-action="ViewStudents">Students</a></li>
                            <li class="breadcrumb-item active">Add</li>
                        </ol>
                    </nav>
                </div>
                <div class="col-md-4 mt-3 mt-md-0">
                    <div class="d-flex flex-wrap gap-2 justify-content-md-end">
                        <a asp-action="ViewStudents" class="btn btn-outline-light btn-sm">
                            <i class="fas fa-list me-1"></i>View All
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- ============================================
         MAIN FORM
         ============================================ -->
    <div class="row justify-content-center">
        <div class="col-12 col-md-8 col-lg-7">
            <div class="form-card-modern">
                <div class="card-header">
                    <h5 class="mb-0 fw-bold">
                        <i class="fas fa-user-graduate text-success me-2"></i>Student Information
                        <span class="badge bg-success ms-2">New</span>
                    </h5>
                </div>
                <div class="card-body">

                    <!-- Success Alert -->
                    @if (TempData["Success"] != null)
                    {
                        <div class="success-alert-modern" id="successAlert">
                            <h6><i class="fas fa-check-circle me-2" style="color: #22c55e;"></i>@TempData["Success"]</h6>
                            <div class="credentials-box">
                                <div class="cred-item">
                                    <span class="label"><i class="fas fa-id-card"></i> Admission:</span>
                                    <span class="value admission">@TempData["AdmissionNumber"]</span>
                                </div>
                                <div class="cred-item">
                                    <span class="label"><i class="fas fa-key"></i> Password:</span>
                                    <span class="value pass">@TempData["Password"]</span>
                                    <button class="btn-copy" onclick="copyText('@TempData["Password"]')" title="Copy password">
                                        <i class="fas fa-copy"></i> Copy
                                    </button>
                                </div>
                            </div>
                            <small class="text-muted d-block mt-2">
                                <i class="fas fa-info-circle me-1"></i>
                                Please save these credentials and share them securely with the student.
                            </small>
                            <button type="button" class="btn-close" data-bs-dismiss="alert" style="position: absolute; top: 16px; right: 16px;"></button>
                        </div>
                    }

                    <!-- Validation Summary -->
                    <div asp-validation-summary="ModelOnly" class="alert alert-danger"></div>

                    <!-- Form -->
                    <form method="post" id="addStudentForm" novalidate>
                        @Html.AntiForgeryToken()

                        <div class="row g-3">
                            <!-- Full Name -->
                            <div class="col-12 col-sm-6">
                                <label asp-for="FullName" class="form-label-modern">Full Name</label>
                                <div class="input-group input-group-modern">
                                    <span class="input-group-text"><i class="fas fa-user"></i></span>
                                    <input asp-for="FullName" class="form-control form-control-modern" placeholder="Enter full name" required />
                                </div>
                                <span asp-validation-for="FullName" class="text-danger small"></span>
                            </div>

                            <!-- Email -->
                            <div class="col-12 col-sm-6">
                                <label asp-for="Email" class="form-label-modern">Email Address</label>
                                <div class="input-group input-group-modern">
                                    <span class="input-group-text"><i class="fas fa-envelope"></i></span>
                                    <input asp-for="Email" class="form-control form-control-modern" type="email" placeholder="Enter email" required />
                                </div>
                                <span asp-validation-for="Email" class="text-danger small"></span>
                            </div>

                            <!-- Class -->
                            <div class="col-12 col-sm-6">
                                <label asp-for="Class" class="form-label-modern">Class</label>
                                <div class="input-group input-group-modern">
                                    <span class="input-group-text"><i class="fas fa-graduation-cap"></i></span>
                                    <input asp-for="Class" class="form-control form-control-modern" placeholder="Enter class" required />
                                </div>
                                <span asp-validation-for="Class" class="text-danger small"></span>
                            </div>

                            <!-- Section -->
                            <div class="col-12 col-sm-6">
                                <label asp-for="Section" class="form-label-modern">Section</label>
                                <div class="input-group input-group-modern">
                                    <span class="input-group-text"><i class="fas fa-layer-group"></i></span>
                                    <input asp-for="Section" class="form-control form-control-modern" placeholder="Enter section" />
                                </div>
                                <span asp-validation-for="Section" class="text-danger small"></span>
                            </div>

                            <!-- Date of Birth -->
                            <div class="col-12 col-sm-6">
                                <label asp-for="DateOfBirth" class="form-label-modern">Date of Birth</label>
                                <div class="input-group input-group-modern">
                                    <span class="input-group-text"><i class="fas fa-calendar"></i></span>
                                    <input asp-for="DateOfBirth" class="form-control form-control-modern" type="date" required />
                                </div>
                                <span asp-validation-for="DateOfBirth" class="text-danger small"></span>
                            </div>

                            <!-- Teacher -->
                            <div class="col-12 col-sm-6">
                                <label asp-for="TeacherId" class="form-label-modern">Assign Teacher</label>
                                <div class="input-group input-group-modern">
                                    <span class="input-group-text"><i class="fas fa-chalkboard-teacher"></i></span>
                                    <select asp-for="TeacherId" class="form-select form-select-modern" asp-items="ViewBag.Teachers">
                                        <option value="">-- Select Teacher --</option>
                                    </select>
                                </div>
                                <span asp-validation-for="TeacherId" class="text-danger small"></span>
                            </div>
                        </div>

                        <!-- Auto-Generated Credentials -->
                        <div class="auto-gen-section mt-4">
                            <div class="section-title">
                                <i class="fas fa-robot"></i>
                                Auto-Generated Credentials
                                <span class="badge bg-success ms-2">Auto</span>
                            </div>

                            <div class="row g-3">
                                <!-- Admission Number -->
                                <div class="col-12 col-sm-6">
                                    <label class="fw-bold small text-muted">Admission Number</label>
                                    <div class="input-group">
                                        <span class="input-group-text"><i class="fas fa-id-card"></i></span>
                                        <input asp-for="AdmissionNumber" id="admissionNumberInput" class="credential-field" readonly />
                                    </div>
                                </div>

                                <!-- Password -->
                                <div class="col-12 col-sm-6">
                                    <label class="fw-bold small text-muted">Password</label>
                                    <div class="input-group">
                                        <span class="input-group-text"><i class="fas fa-key"></i></span>
                                        <input asp-for="Password" id="passwordInput" class="credential-field" readonly type="password" />
                                        <button class="btn-toggle-password" type="button" onclick="togglePasswordVisibility()" title="Toggle visibility">
                                            <i class="fas fa-eye" id="eyeIcon"></i>
                                        </button>
                                        <button class="btn-regenerate" type="button" onclick="regenerateCredentials()" title="Regenerate">
                                            <i class="fas fa-sync-alt me-1"></i> Regenerate
                                        </button>
                                    </div>
                                    <small class="text-muted">
                                        <i class="fas fa-info-circle me-1"></i>
                                        Double-click the password to copy it
                                    </small>
                                </div>
                            </div>
                        </div>

                        <!-- Info Note -->
                        <div class="alert alert-info d-flex align-items-center gap-2 mt-3" style="border-radius: 10px; border-left: 4px solid #22c55e;">
                            <i class="fas fa-info-circle fs-5" style="color: #22c55e;"></i>
                            <div>
                                <strong>Note:</strong> Admission Number and Password are auto-generated.
                                Click <strong>Regenerate</strong> for a new password.
                            </div>
                        </div>

                        <!-- Action Buttons -->
                        <div class="action-buttons-modern">
                            <a asp-action="Dashboard" class="btn btn-secondary">
                                <i class="fas fa-times me-2"></i>Cancel
                            </a>
                            <button type="submit" class="btn btn-success" id="submitBtn">
                                <i class="fas fa-save me-2"></i>Add Student
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

<!-- ============================================
     SCRIPTS
     ============================================ -->
@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }

    <script>
        // ============================================
        // REGENERATE CREDENTIALS
        // ============================================
        function regenerateCredentials() {
            var btn = document.querySelector('.btn-regenerate');
            var originalHtml = btn.innerHTML;
            btn.innerHTML = '<i class="fas fa-spinner fa-spin"></i>';
            btn.disabled = true;

            fetch('@Url.Action("GenerateCredentials", "Admin")', {
                method: 'GET',
                headers: {
                    'X-Requested-With': 'XMLHttpRequest'
                }
            })
            .then(response => response.json())
            .then(data => {
                document.getElementById('admissionNumberInput').value = data.admissionNumber;
                document.getElementById('passwordInput').value = data.password;
                document.getElementById('passwordInput').setAttribute('type', 'password');
                document.getElementById('eyeIcon').className = 'fas fa-eye';

                btn.innerHTML = '<i class="fas fa-check"></i>';
                btn.disabled = false;
                setTimeout(function() {
                    btn.innerHTML = originalHtml;
                }, 1500);
            })
            .catch(function() {
                alert('Failed to regenerate credentials. Please try again.');
                btn.innerHTML = originalHtml;
                btn.disabled = false;
            });
        }

        // ============================================
        // TOGGLE PASSWORD VISIBILITY
        // ============================================
        function togglePasswordVisibility() {
            var passwordInput = document.getElementById('passwordInput');
            var eyeIcon = document.getElementById('eyeIcon');

            if (passwordInput.getAttribute('type') === 'password') {
                passwordInput.setAttribute('type', 'text');
                eyeIcon.className = 'fas fa-eye-slash';
            } else {
                passwordInput.setAttribute('type', 'password');
                eyeIcon.className = 'fas fa-eye';
            }
        }

        // ============================================
        // COPY TO CLIPBOARD
        // ============================================
        function copyText(text) {
            navigator.clipboard.writeText(text).then(function() {
                showTooltip('✓ Copied!');
            }).catch(function() {
                // Fallback
                var tempInput = document.createElement('input');
                tempInput.value = text;
                document.body.appendChild(tempInput);
                tempInput.select();
                document.execCommand('copy');
                tempInput.remove();
                showTooltip('✓ Copied!');
            });
        }

        function showTooltip(message) {
            var tooltip = document.createElement('div');
            tooltip.className = 'copy-tooltip';
            tooltip.textContent = message;
            document.body.appendChild(tooltip);
            setTimeout(function() {
                tooltip.style.opacity = '0';
                setTimeout(function() {
                    tooltip.remove();
                }, 500);
            }, 1500);
        }

        // ============================================
        // DOM CONTENT LOADED
        // ============================================
        document.addEventListener('DOMContentLoaded', function() {
            // Double-click to copy password
            var passwordInput = document.getElementById('passwordInput');
            if (passwordInput) {
                passwordInput.addEventListener('dblclick', function() {
                    copyText(this.value);
                });
            }

            // Form submit loading state
            var form = document.getElementById('addStudentForm');
            var submitBtn = document.getElementById('submitBtn');

            if (form && submitBtn) {
                form.addEventListener('submit', function() {
                    submitBtn.disabled = true;
                    submitBtn.innerHTML = '<i class="fas fa-spinner fa-spin me-2"></i> Adding...';
                });
            }

            // Auto-hide success alert
            var successAlert = document.getElementById('successAlert');
            if (successAlert) {
                setTimeout(function() {
                    var alert = new bootstrap.Alert(successAlert);
                    alert.close();
                }, 8000);
            }

            // Auto-focus on first input
            var firstInput = document.querySelector('input[asp-for="FullName"]');
            if (firstInput) {
                firstInput.focus();
            }

            // Keyboard shortcut: Ctrl+Enter
            document.addEventListener('keydown', function(e) {
                if ((e.ctrlKey || e.metaKey) && e.key === 'Enter') {
                    var form = document.getElementById('addStudentForm');
                    if (form && document.activeElement && document.activeElement.closest('form') === form) {
                        e.preventDefault();
                        form.submit();
                    }
                }
            });
        });
    </script>
}